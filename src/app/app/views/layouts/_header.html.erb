<nav class="navbar navbar-inverse navbar-static-top" id="application-header">
    <div class="container">
        <div class="navbar-header" id="expanded-menu">
            <%= image_tag 'logo.png', id: 'spottapp-logo' %>
            <% if user_signed_in? %>
                <%= link_to 'Spottapp', user_path(current_user), class: 'navbar-brand' %>
            <% else %>
                <%= link_to 'Spottapp', root_url, class: 'navbar-brand' %>
            <% end %>
        </div>
    
        <!-- Conteudo exibido em telas maiores e que serao escondidos em telas menores -->
        <div class="collapse navbar-collapse" id="expanded-menu">
        
        <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    bem vindo, <%= current_user.first_name %> <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                    <li>
                        <%= link_to 'Sair', sair_path, method: :delete %>
                    </li>
                    </ul>
                </li>
            <% end %>
    
        </ul>
    
        </div>
    </div>

</nav>