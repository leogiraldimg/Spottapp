<%= form_for([@spotted, @spotted.comment.build]) do |f| %>
    <%= render 'layouts/error_messages', object: f.object %>
    <div class="row" id="comment-area">
        <div class="col-xs-9 col-md-9" id="new-comment">
            <div class="form-group">
                <%= f.text_field :content, class: 'form-control' %>
            </div>
        </div>
        <div class="col-xs-3 col-md-3" id="button-send">
            <%= f.submit 'Mandar comentário', class: 'btn btn-primary' %>
        </div>
    </div>
<% end %>